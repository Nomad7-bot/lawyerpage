import { NextResponse } from "next/server";

/**
 * 예약 API (Phase 2 에서 구현)
 */
export async function GET() {
  return NextResponse.json({ message: "reservations API - placeholder" });
}

export async function POST() {
  return NextResponse.json({ message: "reservations API - placeholder" });
}
